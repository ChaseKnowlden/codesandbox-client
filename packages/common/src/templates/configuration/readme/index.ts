import { ConfigurationFile } from '../types';

const config: ConfigurationFile = {
  title: 'Readme.md',
  type: 'readme',
  description: 'Readme of the Sandbox',
  moreInfoUrl: 'https://www.markdownguide.org/',
  noHeader: true,
};

export default config;
